# تقرير إصلاح إدارة الفصول والشُعب

التاريخ: 2026-09-18

## النتيجة

تم إغلاق مسار إدارة الشُعب على قاعدة البيانات والواجهة المحلية، مع الحفاظ على السجلات الحالية، ومنع ربط شعبة بفصل أو مؤسسة غير صحيحة. لم يتم تنفيذ push أو deploy أو أي تغيير على Production.

## ما تم تنفيذه

### قاعدة البيانات

تمت إضافة migration جديدة فقط:

`supabase/migrations/20260924140000_class_section_scope_and_exam_visibility.sql`

التغييرات:

- إضافة `institution_id`, `academic_year_id`, `grade_level_id`, `branch_id`, `code`, و`updated_at` إلى `sections` عند الحاجة باستخدام `IF NOT EXISTS`.
- ملء نطاق الشُعب القديمة من الفصل الأب دون حذف أو إسقاط أي سجل.
- جعل `sections.institution_id` إلزاميًا وإضافة المفاتيح الخارجية المناسبة.
- منع تكرار اسم الشعبة داخل نفس المؤسسة والفصل عبر `sections_institution_parent_name_unique`.
- إضافة trigger يضمن أن المؤسسة والعام والصف والفرع في الشعبة يطابقون الفصل الأب.
- إضافة trigger يضمن أن تسجيل الطالب يحمل نطاق الفصل الصحيح، وأن `section_id` يتبع نفس الفصل والمؤسسة والعام والصف.
- إعادة تعريف `is_exam_assigned_to_current_student` بحيث:
  - امتحان الفصل يطابق طالبًا له تسجيل نشط في نفس الفصل.
  - امتحان الشعبة يطابق الشعبة المحددة فقط، حتى لو كان `class_id` موجودًا معه.
  - لا تُستخدم تسجيلات من مؤسسة أخرى.
- تحديث RLS على `sections` و`class_students` لعزل المؤسسات، مع الإبقاء على صلاحية المدرس حسب التكليف.

### الواجهات

تم تحديث:

- `src/views/AcademicSetup.tsx`
  - إضافة تبويب الشُعب.
  - عرض اسم الشعبة وكودها والفصل/الصف/العام/الفرع المرتبط.
  - إضافة وتعديل وتعطيل الشعبة.
  - إلزام اختيار الفصل الأب قبل الحفظ.
- `src/views/SIS.tsx`
  - تحميل العام الدراسي ونطاق الشُعب من المؤسسة الحالية.
  - فلترة الفصول والشُعب حسب العام والفرع والصف والفصل.
  - حفظ `institution_id`, `academic_year_id`, `grade_level_id`, `class_id`, و`section_id` في `class_students`.
  - إظهار رسالة واضحة عند عدم وجود شُعب.
  - منع حفظ تسجيل بفصل له شعب نشطة بدون اختيار الشعبة.

## الاختبارات

تمت إضافة:

- `security/class-section-management-static.test.mjs`
- `security/class-section-local-fixture.sql`

الـ fixture المحلي اختبر فعليًا:

- مؤسستين A وB.
- فصلًا في كل مؤسسة، وشعبتين في فصل المؤسسة A.
- طالبًا في كل شعبة، وطالبًا في مؤسسة B.
- ظهور الشعبة الصحيحة في تسجيل الطالب.
- منع ظهور بيانات A للمؤسسة B.
- منع تكرار الشعبة.
- ظهور امتحان الفصل لطالبَي الشعبتين.
- ظهور امتحان الشعبة لطالب الشعبة الصحيحة فقط.
- ظهور الامتحانين للمدرس المكلّف بالمادة والفصل.

النتائج:

- `npm run test:phase1`: ناجح، 15/15.
- `npm run typecheck`: ناجح.
- `npm run lint`: ناجح، 0 أخطاء، مع 31 تحذيرًا قديمًا موجودًا بالمشروع.
- `npm run build`: ناجح.
- `npx supabase db push --local --include-all --yes`: migration طُبقت محليًا بنجاح.
- `npx supabase db lint --local`: ناجح، والتحذيرات الموجودة تخص دوال سابقة وليست migration الجديدة.
- fixture قاعدة البيانات المحلية: `CLASS_SECTION_FIXTURE_PASS`.
- `git diff --check`: ناجح، مع تحذيرات line endings فقط.

## المتبقي

المتبقي هو Browser walkthrough بصري من حساب مدير مؤسسة وحساب طالب وحساب مدرس للتأكد من ترتيب الشاشة وسلوك القوائم في المتصفح. بيئة المتصفح المدمجة لم تكن متاحة أثناء التنفيذ، لذلك لم أعتبر هذا الجزء مُغلقًا باختبار آلي.

## النشر

لم يتم تنفيذ push أو deploy أو تطبيق migration على Supabase البعيدة أو Docker/Production بناءً على طلبك.
